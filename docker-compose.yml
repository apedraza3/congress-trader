services:
  app:
    build: .
    container_name: congress-trader
    restart: unless-stopped
    ports:
      - "127.0.0.1:5051:5051"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
